<!DOCTYPE html>
<html>
  <head>
    <!-- application_helper.rb -->
    <title><%= full_title(yield(:title))%></title>
    <%= render 'layouts/html_shim' %>
    <%= render 'layouts/rails_tags' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
