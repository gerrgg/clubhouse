<% provide(:title, @user.name) %>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1><%= @user.name %></h1>
    <p class="text-muted">
      <%= @user.email %>
    </p>
  </div>
</div>
